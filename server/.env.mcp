# MCP Integration Environment Variables
# Copy this to your main .env file or source it before starting the server

# Required for MCP webhook signature verification
MCP_SHARED_SECRET="mcp-secret-key-2024"

# Optional: Local storage path for downloaded contracts
LOCAL_CONTRACT_STORAGE="/tmp/contracts"

# Note: Google credentials are managed by the MCP server itself
# The MCP server looks for credentials in:
# - mcp/gdrive_mcp/gdrive-mcp-server/credentials/.gdrive-server-credentials.json
# - mcp/gdrive_mcp/gdrive-mcp-server/credentials/gcp-oauth.keys.json